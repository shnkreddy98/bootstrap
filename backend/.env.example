# ============================================
# Backend Environment Variables
# ============================================

# Backend Server Port
PORT=3000

# PostgreSQL Database Connection
# Format: postgresql://user:password@host:port/database
# Use this URL when running with docker-compose (bun run db:up from root)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/myapp

# JWKS Authentication Configuration
# JWKS_URI: URL to fetch the JSON Web Key Set for JWT verification
JWKS_URI=https://your-auth-provider.com/.well-known/jwks.json

# JWT_ISSUER: Expected issuer claim in the JWT
JWT_ISSUER=https://your-auth-provider.com

# JWT_AUDIENCE: Expected audience claim in the JWT
JWT_AUDIENCE=your-api-audience
