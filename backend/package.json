{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --env-file=../.env --watch src/index.ts",
    "start": "bun run --env-file=../.env src/index.ts",
    "db:migrate": "bun run --env-file=../.env src/migrations/migrate.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.7.9",
    "@hono/zod-validator": "^0.4.1",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.10"
  }
}
