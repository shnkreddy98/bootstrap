/**
 * Zustand Store Template - UI State Only
 *
 * This file is a TEMPLATE for when you need client-side UI state.
 * Server state is managed by Tanstack Query in lib/queries.ts
 *
 * IMPORTANT: This store is not currently used in the application.
 * Uncomment and modify the code below when you need UI state management.
 *
 * Examples of UI state (what belongs here):
 * - Modal open/closed
 * - Sidebar expanded/collapsed
 * - Current theme selection
 * - Form draft values
 * - UI filters that don't sync to URL
 *
 * Examples of server state (use Tanstack Query instead):
 * - Todos, users, posts (any data from API)
 * - Cache invalidation
 * - Loading/error states for API calls
 *
 * Usage:
 * 1. Uncomment the code below
 * 2. Define your UI state interface
 * 3. Create your store with actions
 * 4. Import in components: import { useUIStore } from '@/store'
 */

// import { create } from 'zustand'
//
// type UIStore = {
//   // Example: Modal state
//   isModalOpen: boolean
//   openModal: () => void
//   closeModal: () => void
// }
//
// export const useUIStore = create<UIStore>((set) => ({
//   isModalOpen: false,
//   openModal: () => set({ isModalOpen: true }),
//   closeModal: () => set({ isModalOpen: false }),
// }))
