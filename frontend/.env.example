# ============================================
# Frontend Environment Variables
# ============================================

# API Base URL
# In production (single process), API is on same host (leave empty)
# In dev, backend is on :3000, frontend on :5173
VITE_API_URL=http://localhost:3000

# External Auth URL (optional)
# Used for redirecting on 401/403 errors
EXTERNAL_AUTH_URL=https://auth.example.com/login
