# ============================================
# Frontend Environment Variables
# ============================================

# Backend URL for development proxy (only needed when running frontend dev server)
# Should match backend PORT
# Leave empty or omit for default http://localhost:3000
VITE_BACKEND_URL=http://localhost:3000

# Note: API calls use relative URLs (proxied in dev via vite.config.ts)
# Runtime configuration is fetched from backend via /api/config
